'use client';

// This component is no longer used as the app only has a light theme
// Implementing an empty component to prevent build errors

import React from 'react';

const ThemeToggle: React.FC = () => {
  return null;
};

export default ThemeToggle; 